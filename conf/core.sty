%-----------------------------------------------------------
%
% Usage:
%
% Place this file in the same directory as your .tex 
% document and write \usepackage{config} after
% \documentclass[...]{...} or \RequirePackage{config}
% before \documentclass[...]{...} in your .tex document
%
%___________________________________________________________
%

%___________________________________________________________
%
% Language settings and character sets
%
% Allows us to use Swedish characters, å, ä, ö.
% The sections generated by LaTeX (such as Table of Contents
% and Abstract) will be in English.
%___________________________________________________________
\RequirePackage[T1]{fontenc}
\catcode`\_=12 % Use \sb for subscripts.
\RequirePackage[utf8]{inputenc}
\RequirePackage[english]{babel}

% Font: Palatino
\RequirePackage{mathpazo} 
\linespread{1.05}

% Provides some typographic improvements over default settings
\RequirePackage{microtype} 

% Allows for nice pageheaders (and footers)
\RequirePackage{fancyhdr}

% For aligning images with \FloatBarrier
\RequirePackage{placeins} 

% For nice-looking tables
\RequirePackage{booktabs}

% Remove paragraph indents
\RequirePackage{parskip}

% Allows for optional arguments to be passed to
% the \includegraphics command
\RequirePackage{graphicx}

% Adjusts page margins
\RequirePackage{geometry}

% Style links - internal document links as well as URLs.
\RequirePackage{hyphenat}
\RequirePackage{hyperref}
\hypersetup{
  colorlinks = true, % Colours links instead of ugly boxes
  urlcolor = blue, % Colour for external hyperlinks
  linkcolor = blue, % Colour of internal links
  citecolor = red % Colour of citations
}

% \marginnote{...} For making small notes in the margin.
% Should not be abused as it makes the page feel cluttered.
\RequirePackage{marginnote}

% Adjusts the font used in the margin
\renewcommand*{\marginfont}{\footnotesize}

% An environment for nice looking quotes
\makeatletter
\newenvironment{chapquote}[2][2em]
  {\setlength{\@tempdima}{#1}%
    \def\chapquote@author{#2}%
    \parshape 1 \@tempdima \dimexpr\textwidth-2\@tempdima\relax}
  {\par\normalfont\hfill\ \chapquote@author\hspace*{\@tempdima}\par}
  \makeatother

  
% Instead of numbering footnotes use symbols. More legible.
\renewcommand*{\thefootnote}{\fnsymbol{footnote}}

% Allows verbatim text in places such as the footnotes
% section. Enables delimiter-configuration for the
% \Verb|...| command
\RequirePackage{fancyvrb}

\RequirePackage{showexpl}

% Multi-line comments using \begin{comment}
\RequirePackage{verbatim}

\RequirePackage{varwidth} % Adjust minipages
\RequirePackage{titlesec} % More granular control of sections.

\RequirePackage{xspace} % Fixes spacing after margins.
\RequirePackage{mparhack} % "marginpar" done The Right Way (TM).
\RequirePackage{fixltx2e} % Fixes some LaTeX stuff. 

%___________________________________________________________
%
% Qtree
%
% Great for drawing small trees of various kinds.
% Documentation:
% http://www.ling.upenn.edu/advice/latex/qtree/qtreenotes.pdf
% Common usage:
% \Tree [.Root child1 [.child2 grandChild1 grandChild2 ] ]
%___________________________________________________________
\RequirePackage{qtree}
%___________________________________________________________
%
% BibLaTeX
%
% Great reference system
% Documentation:
% http://mirrors.ctan.org/macros/latex/contrib/biblatex/doc/biblatex.pdf
%___________________________________________________________
\RequirePackage[style = ieee, urldate =comp, backend=bibtex]{biblatex}
\addbibresource{references.bib}

\RequirePackage{conf/commands}
\RequirePackage{conf/math}
